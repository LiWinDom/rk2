set(CPACK_PACKAGE_NAME "Interpreter")
if (WIN32)
  set(CPACK_OUTPUT_FILE_PREFIX "../bin/${CMAKE_BUILD_ARCHITECTURE}/${CMAKE_BUILD_TYPE}/")
else ()
  set(CPACK_OUTPUT_FILE_PREFIX "../bin/${CMAKE_BUILD_TYPE}/")
endif ()
set(CPACK_PACKAGE_FILE_NAME "../bin/${CMAKE_BUILD_ARCHITECTURE}/${CMAKE_BUILD_TYPE}/Interpreter-${CPACK_PACKAGE_VERSION}")
set(CPACK_PACKAGE_CONTACT "LiWinDom")

set(CPACK_SOURCE_GENERATOR "TGZ;ZIP")
if(LINUX)
  set(CPACK_GENERATOR "DEB;RPM")
elseif(WIN32)
  set(CPACK_GENERATOR "MSI")
elseif(APPLE)
  set(CPACK_GENERATOR "DragNDrop")
endif()

include(CPack)
